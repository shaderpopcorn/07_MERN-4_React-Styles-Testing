import { MainSection, Box, Inner } from "./Main.styled";

const Main = () => {
  return (
    <MainSection>
      <Box>
        <Inner>
          <span>VR + AR + 3D + 2D</span>
        </Inner>
        <Inner>
          <span>VR + AR + 3D + 2D</span>
        </Inner>
      </Box>
    </MainSection>
  );
};

export default Main;
